# New App

Invoke the **new-app** skill to start a new application in the monorepo.

Runs a structured discovery conversation, then iterates on APP_SPEC.md, BDD feature files, and ADRs. Does NOT scaffold code.

Pass along any user arguments: $ARGUMENTS
